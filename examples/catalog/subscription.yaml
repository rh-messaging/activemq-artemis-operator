apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: arkmq-org-broker-operator-subscription
  namespace: operators
spec:
  channel: stable
  name: arkmq-org-broker-operator
  source: arkmq-org-broker-operator-catalog-source
  sourceNamespace: operators
